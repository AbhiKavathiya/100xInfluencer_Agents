import LoginPage from "@/sections/auth/LoginPage";
import React from "react";

export const metadata = {
  title: "Sign In",
  description: "Secure login to your AI platform account.",
};

const page = () => {
  return <LoginPage />;
};

export default page;
